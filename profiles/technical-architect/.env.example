# 技术架构师 角色所需的凭据。
# 复制为 .env 并填入真实值；.env 已被 .gitignore 排除，绝不会被提交。
#
#   cp profiles/technical-architect/.env.example profiles/technical-architect/.env
#
# 模型配置见同目录 config.yaml（model.default / model.provider）。
DEEPSEEK_API_KEY=
