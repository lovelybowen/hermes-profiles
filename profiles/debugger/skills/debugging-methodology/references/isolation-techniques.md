# 隔离技术

## 二分查找（分而治之）

用于识别导致故障的组件、提交或输入：

1. 将搜索空间一分为二。
2. 分别测试两部分。
3. 对失败的一半递归执行。
4. 隔离到单个单元时停止。

## 组件隔离

尽可能独立测试组件：
- 模拟外部依赖。
- 使用受控输入隔离测试组件。
- 测试集成前先验证组件行为。

## 最小复现

为排除无关因素：
1. 从尽可能简单的复现用例开始。
2. 每次增加一个复杂因素，直至故障复现。
3. 最小复现会揭示故障发生的必要条件。
